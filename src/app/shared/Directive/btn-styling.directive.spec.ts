import { BtnStylingDirective } from './btn-styling.directive';

describe('BtnStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnStylingDirective();
    expect(directive).toBeTruthy();
  });
});
